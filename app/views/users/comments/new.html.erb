<section class="comments new">
  <div class="page-header">
    <h1>New comment for <%= @guest.name %></h1>
  </div>
  <%= render partial: 'form', locals: { url: wedding_guest_comments_path(@wedding, @guest) } %>
</section>
