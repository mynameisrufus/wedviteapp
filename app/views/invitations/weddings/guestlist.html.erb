<table class="table">
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @guests.each_with_index do |guest, index| %>
    <tr>
      <th scope="row"><%= index + 1 %></th>
      <td><%= guest.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>
