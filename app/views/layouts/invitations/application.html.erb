<!DOCTYPE html>
<html>
  <head>
    <title><%= @wedding.title %> - WedVite</title>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
    <link href="/favicon.png" rel="shortcut icon" type="image/png" />
    <%= stylesheet_link_tag "invitations/application" %>
    <!--[if lt IE 9]>
      <%= javascript_include_tag "//html5shiv.googlecode.com/svn/trunk/html5.js" %>
    <![endif]-->
    <%= yield :head %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-light bg-faded navbar-fixed-top hidden-md-up">
      <button class="navbar-toggler pull-xs-right " type="button" data-toggle="offcanvas" data-target="#sidebar">
        &#9776;
      </button>
      <a class="navbar-brand"><%= controller.page_title %></a>
    </nav>

    <nav class="navbar navbar-light bg-faded navbar-fixed-top hidden-sm-down">
      <%= link_to guest_path(@guest.token), class: "navbar-brand text-truncate pull-sm-right" do %>
        <%= avatar(@guest) %> <%= @guest.name %>
      <% end %>
    </nav>

    <div class="container-fluid">
      <div class="row row-offcanvas row-offcanvas-right">
        <div class="col-xs-6 col-md-3 sidebar-offcanvas" id="sidebar">
          <% unless @preview_mode %>
            <%= render "layouts/invitations/menu" %>
          <% end %>
        </div>
        <div class="col-xs-12 col-md-9">
          <%= render partial: "layouts/users/flash" %>
          <%= yield %>
        </div>
      </div>
    </div>

    <footer class="wd-footer">
      <p><%= @wedding.title %></p>
    </footer>

    <%= javascript_include_tag "invitations/application" %>
    <%= javascript_include_tag 'http://maps.googleapis.com/maps/api/js?sensor=true&libraries=places' %>
    <%= render partial: 'layouts/analytics'  if Rails.env.production? %>
  </body>
</html>
