<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <% unless user_signed_in? %>
        <h1 class="brand">Wed Vite</h1>
      <% else %>
        <a class="brand" href="/">Wed Vite</a>
        <ul class="nav pull-right">
          <li><%= link_to feedback_new_path do %><i class="icon-envelope icon-white"></i> Feedback and Support<% end %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Sign out", destroy_user_session_path, method: 'delete' %></li>
              <li><%= link_to "View profile", edit_user_registration_path %></li>
            </ul>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</div>
